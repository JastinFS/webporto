/** Thin wrapper kept for backwards-compatible imports.
 *  Real implementation lives in ../smooth/lenis (Lenis-aware). */
export { scrollToId as smoothScrollTo, scrollToTop } from '../smooth/lenis';
